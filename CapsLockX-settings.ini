; CapsLockX settings
; - No administrator rights required; keep this folder writable.
; - Override keys under [Keys], then press CapsLock+F5 to reload.
; - Logs: CapsLockX-exit.log, CapsLockX-error.log (same folder as CapsLockX.ahk)
;
; Examples:
;   caps_q=none
;   caps_f7=reload
;
; Action names: moveLeft, moveRight, copy, paste, cut, none,
;   winbind_activate(1), winbind_binding(1), reload, winPin, ...

[Global]

[Keys]
